
function fn(someObj, array) {
    // const name = someObj.name; 
    const { name, age, date } = someObj;

    // const first = array[0];
    // const second = array[1];
    const [ first, second ] = array;

}